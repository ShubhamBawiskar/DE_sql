USE customer_orders;
SELECT * FROM customer_orders;

-- =================================================================================================================================
-- 1. Display all orders where the delivery partner is NULL.
-- =================================================================================================================================
SELECT * FROM customer_orders WHERE delivery_partner IS NULL;

-- =================================================================================================================================
-- 2. Count how many orders do not have a delivery partner.
-- =================================================================================================================================
SELECT COUNT(*)
FROM customer_orders
WHERE delivery_partner IS NULL;

-- =================================================================================================================================
-- 3. Count how many orders have a discount.
-- =================================================================================================================================
SELECT COUNT(*)
AS discounted
FROM customer_orders
WHERE discount IS NOT NULL;

-- OR

SELECT COUNT(*) as discounted
FROM customer_orders;

-- =================================================================================================================================
-- 4. Display the total price before discount and after discount.
-- =================================================================================================================================
SELECT * FROM customer_orders;

SELECT 
    unit_price AS pre, 
    unit_price- COALESCE(discount, 0) AS post
FROM customer_orders;

-- =================================================================================================================================
-- 5. Display GST (18%) for every order.
-- =================================================================================================================================
SELECT 
    unit_price,
    unit_price*0.18
AS GST
FROM customer_orders;

-- =================================================================================================================================
-- 6. Increase every product price by ₹500.
-- =================================================================================================================================
SELECT 
    unit_price,
    unit_price+500 
AS increased_price
FROM customer_orders;

-- =================================================================================================================================
-- 7. Display orders whose status is not 'Delivered'.
-- =================================================================================================================================
SELECT * FROM customer_orders;

SELECT * 
FROM customer_orders
WHERE order_status != 'delivered';

-- =================================================================================================================================
-- 8. Display Electronics orders whose quantity is greater than 2.
-- =================================================================================================================================
SELECT * FROM customer_orders
WHERE 
    category='electronics' AND
    quantity>2;

-- =================================================================================================================================
-- 9. Display orders that are NOT Delivered.
-- =================================================================================================================================
SELECT * 
FROM customer_orders
WHERE order_status <> 'delivered';

-- =================================================================================================================================
-- 10. Display orders where Category = Electronics AND City = Bhubaneswar AND Quantity > 1
-- =================================================================================================================================
SELECT * FROM customer_orders
WHERE 
    category='ELECTRONICS' AND
    city='bhubaneswar' AND
    quantity>1;

-- =================================================================================================================================
-- 11. Count total orders.
-- =================================================================================================================================
SELECT COUNT(order_id) FROM customer_orders;

-- =================================================================================================================================
-- 12. Find highest product price.
-- =================================================================================================================================
SELECT MAX(unit_price) FROM customer_orders;

-- =================================================================================================================================
-- 13. Find total revenue after discount.
-- =================================================================================================================================
SELECT SUM(unit_price-discount) FROM customer_orders;

SELECT SUM( COALESCE(unit_price,0) - COALESCE(discount,0)) 
AS discounted
FROM customer_orders;                   -- safest method

-- =================================================================================================================================
-- 14. Count pending orders.
-- =================================================================================================================================
SELECT COUNT(order_status)
FROM customer_orders
WHERE order_status = 'pending';

-- =================================================================================================================================
-- 15. Find the total GST collected if GST is 18% on all orders. 
-- =================================================================================================================================
SELECT SUM(COALESCE(unit_price, 0)*0.18) FROM customer_orders;

SELECT SUM(COALESCE(unit_price, 0))*0.18 FROM customer_orders;

